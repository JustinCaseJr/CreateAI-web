export default {async fetch(request,env){const url=new URL(request.url);if(request.method==='OPTIONS')return new Response(null,{headers:cors()});if(url.pathname==='/api/generate-image'&&request.method==='POST'){if(!env.AI)return json({error:'Workers AI binding AI is not configured.'},500);try{const b=await request.json(),prompt=typeof b.prompt==='string'?b.prompt.trim():'';if(!prompt)return json({error:'Prompt is required.'},400);if(prompt.length>2048)return json({error:'Prompt is too long.'},400);const out=await env.AI.run('@cf/black-forest-labs/flux-1-schnell',{prompt,steps:4});const bin=atob(out.image),bytes=Uint8Array.from(bin,c=>c.charCodeAt(0));return new Response(bytes,{headers:{'content-type':'image/jpeg','cache-control':'no-store',...cors()}})}catch(e){return json({error:e.message||'Generation failed.'},500)}}return env.ASSETS?env.ASSETS.fetch(request):new Response('Create AI is running.',{headers:cors()})}};function cors(){return {'access-control-allow-origin':'*','access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'Content-Type'}}function json(x,s=200){return new Response(JSON.stringify(x),{status:s,headers:{'content-type':'application/json',...cors()}})}
