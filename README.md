# Create AI Web

Multi-file version of the Create AI interface. The frontend has working local controls and calls `/api/generate-image`. The Cloudflare Worker uses a Workers AI binding named `AI`.

Do not put Cloudflare API tokens in browser code.
